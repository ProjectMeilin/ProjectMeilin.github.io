## Install & Setup
http://ntanjerome.org/blog/how-to-setup-github-user-page-with-pelican/

## New Articles & Update to github page
./build.sh
git add .
git commit -m "your update notes"

git checkout gh-pages
git fetch --all
git checkout origin/source -- output
cp -pr output/* ./ && rm -rf output
git add . && git commit -m "update new files to gh-pages"

git checkout master
git merge gh-pages
git push --all






## Update Pages
#source ENV/bin/activate
#git checkout source         ( then editing your articles in content/*.md )

#make html
#make serve                  ( check at localhost:8000 )
#git branch -D gh-pages
#git branch gh-pages
#ghp-import output
#git checkout master
#git merge gh-pages
#git push --all
